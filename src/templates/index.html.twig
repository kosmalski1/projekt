{% include 'header.html.twig' %}

    <h1>{{pageTitle}}</h1>
   {% for post in postArray%}
<img src="{{post.getFilename()}}">
<h6>data dodania : {{post.getTimetamp()}}</h6>
    {% endfor %}
{% include 'footer.html.twig' %}